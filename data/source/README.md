# Controlled Source Data

Place the immutable rebuilt source dataset here:

```text
zimbabwe_2022_all_courts_rebuilt.jsonl
```

Do not edit the uploaded source file in place. Record its SHA-256 hash in `data/manifests/source_manifest.json` and create corrected derivatives under `data/curated/`.

This directory may contain sensitive or restricted material and should not be exposed publicly without documented approval.