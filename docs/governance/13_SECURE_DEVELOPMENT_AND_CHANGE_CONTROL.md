# Secure Development and Change Control

Changes use branches, peer review, automated tests, and protected main branches. Secrets are never committed. Dependencies are pinned or reviewed. Dataset changes require source evidence and audit logs. Security and privacy impact is reviewed for new features, integrations, and deployment environments.

Use `docs/registers/change_register.csv`.
